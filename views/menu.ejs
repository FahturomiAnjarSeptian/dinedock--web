<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Our Menu | DineDock</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        /* HEADER MEWAH */
        .menu-header {
            background: #111;
            padding: 40px 20px;
            text-align: center;
            position: relative; /* Wajib relative agar tombol back bisa absolute */
            border-bottom: 3px solid #c5a059;
            margin-bottom: 40px;
        }
        
        .menu-header h1 {
            font-family: 'Playfair Display', serif;
            color: #c5a059;
            margin: 0;
            font-size: 2.5rem;
            letter-spacing: 2px;
        }
        
        .menu-header p {
            color: #888; margin-top: 5px; font-style: italic;
        }

        /* --- TOMBOL BACK KEREN (Lingkaran Emas) --- */
        .btn-back-custom {
            position: absolute;
            left: 30px;
            top: 50%;
            transform: translateY(-50%); /* Posisi tengah vertikal */
            
            display: flex;
            align-items: center;
            justify-content: center;
            
            width: 45px;
            height: 45px;
            border: 1px solid #c5a059;
            border-radius: 50%; /* Bulat Sempurna */
            color: #c5a059;
            text-decoration: none;
            transition: all 0.3s ease;
            background: rgba(0,0,0,0.5);
        }

        .btn-back-custom:hover {
            background: #c5a059;
            color: #000;
            box-shadow: 0 0 15px rgba(197, 160, 89, 0.6);
            transform: translateY(-50%) scale(1.1);
        }

        /* Responsive HP */
        @media (max-width: 768px) {
            .menu-header h1 { font-size: 1.8rem; margin-top: 40px; }
            .btn-back-custom { top: 20px; left: 20px; transform: none; }
        }

        /* CATEGORY HEADER */
        .category-header { text-align: center; margin: 60px 0 30px 0; position: relative; }
        .category-header h2 {
            display: inline-block; color: #c5a059; font-family: 'Playfair Display';
            font-size: 2rem; background: #1a1a1a; padding: 0 20px; position: relative; z-index: 1; margin: 0;
        }
        .category-header::after {
            content: ''; position: absolute; top: 50%; left: 0; right: 0; height: 1px; background: #333; z-index: 0;
        }
    </style>
</head>
<body>

    <div class="menu-header">
        <a href="<%= user && user.userId ? '/dashboard' : '/' %>" class="btn-back-custom" title="Kembali">
            <i class="fas fa-arrow-left"></i>
        </a>
        <h1>OUR SIGNATURE MENU</h1>
        <p>Taste the Perfection</p>
    </div>

    <div class="container">

        <div class="category-header"><h2>MAIN COURSE</h2></div>
        <div class="menu-grid">
            <% menus.filter(item => item.category === 'food').forEach(item => { %>
                <div class="menu-card">
                    <img src="<%= item.image_url %>" alt="<%= item.name %>" class="menu-img">
                    <div class="menu-info">
                        <h3 class="menu-title"><%= item.name %></h3>
                        <p class="menu-desc"><%= item.description %></p>
                        <div class="menu-price">Rp <%= item.price.toLocaleString('id-ID') %></div>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="category-header"><h2>REFRESHMENT</h2></div>
        <div class="menu-grid">
            <% menus.filter(item => item.category === 'beverage').forEach(item => { %>
                <div class="menu-card">
                    <img src="<%= item.image_url %>" alt="<%= item.name %>" class="menu-img">
                    <div class="menu-info">
                        <h3 class="menu-title"><%= item.name %></h3>
                        <p class="menu-desc"><%= item.description %></p>
                        <div class="menu-price">Rp <%= item.price.toLocaleString('id-ID') %></div>
                    </div>
                </div>
            <% }) %>
        </div>

        <div class="category-header"><h2>DESSERT</h2></div>
        <div class="menu-grid">
            <% menus.filter(item => item.category === 'dessert').forEach(item => { %>
                <div class="menu-card">
                    <img src="<%= item.image_url %>" alt="<%= item.name %>" class="menu-img">
                    <div class="menu-info">
                        <h3 class="menu-title"><%= item.name %></h3>
                        <p class="menu-desc"><%= item.description %></p>
                        <div class="menu-price">Rp <%= item.price.toLocaleString('id-ID') %></div>
                    </div>
                </div>
            <% }) %>
        </div>

    </div>
    <div style="text-align: center; margin-top: 80px; padding: 50px 20px; background: #222; border-radius: 15px; border: 1px solid #333; box-shadow: 0 10px 30px rgba(0,0,0,0.3);">
            
            <h2 style="color: white; margin-bottom: 10px; font-family: 'Playfair Display', serif; letter-spacing: 1px;">
                Ready to Experience?
            </h2>
            
            <p style="color: #888; margin-bottom: 30px; font-size: 1rem;">
                Secure your table and parking spot in advance.
            </p>
            
            <a href="<%= user && user.userId ? '/dashboard' : '/login' %>" class="btn-submit" style="display: inline-block; width: auto; padding: 15px 50px; text-decoration: none; color: black; font-size: 1rem; text-transform: uppercase; letter-spacing: 2px;">
                BOOK TABLE NOW
            </a>

        </div>

    </div>

    <div style="text-align: center; padding: 30px; color: #555; font-size: 0.8rem; background: #0a0a0a; margin-top: 40px; border-top: 1px solid #222;">
        &copy; 2025 DineDock Integrated System.
    </div>

</body>
</html>